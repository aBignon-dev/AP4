<?xml version="1.0" encoding="utf-8"?>

<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="AP4test.Views.EnchereSample.EndEnchere.WinView"
             Title="{Binding Title}"
             xmlns:local="clr-namespace:AP4test.ViewModels.EnchereSample.EndEnchere;assembly=AP4test">
    <RefreshView x:DataType="local:WinViewModel" Command="{Binding LoadItemsCommand}"
                 IsRefreshing="{Binding IsBusy, Mode=TwoWay}">
        <StackLayout>
            <Label Text="Hello, world!"
                   VerticalOptions="CenterAndExpand"
                   HorizontalOptions="CenterAndExpand" />
        </StackLayout>
    </RefreshView>
</ContentPage>