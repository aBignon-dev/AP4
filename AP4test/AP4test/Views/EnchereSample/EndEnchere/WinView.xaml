<?xml version="1.0" encoding="utf-8"?>

<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="AP4test.Views.EnchereSample.EndEnchere.WinView"
             Title="{Binding Title}"
             xmlns:local="clr-namespace:AP4test.ViewModels.EnchereSample.EndEnchere;assembly=AP4test">
    <RefreshView x:DataType="local:WinViewModel" Command="{Binding LoadItemsCommand}"
                 IsRefreshing="{Binding IsBusy, Mode=TwoWay}">
        <StackLayout x:DataType="local:WinViewModel" Padding="20" Margin="20" HorizontalOptions="Center">
            <Label Text="{Binding GgText}"  TextColor="Teal" />
            <Label Text="{Binding  WonText}" TextColor="Teal"/>
            <Label Text="{Binding  InfoFirstLineText}" />
            <Label Text="{Binding   InfoSecondLineText}" />
            <Image
                Source="https://media0.giphy.com/media/PgXcWhrL0gjQlAhHUY/giphy.gif"
                IsAnimationPlaying="True" />
            <Button Command="{Binding ButtonSuivant }" Text="{Binding ButtonSuivantText}"></Button>
        </StackLayout>
    </RefreshView>
</ContentPage>